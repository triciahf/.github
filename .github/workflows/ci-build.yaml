name: CI Build
on:
  workflow_call:

jobs:
  build:
    runs-on: triciahf/sen-v1
    steps:
      - name: Checkout
        uses: actions/checkout@v2
    
      - name: echo
        run: echo "hi again"
